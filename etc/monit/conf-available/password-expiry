# Monitor sudo user password expiry
# Alerts at 30, 20, 10, and 5 days remaining
check program password-expiry
    with path "/usr/local/bin/check-password-expiry.sh"
    every 1440 cycles
    if status != 0 then alert
